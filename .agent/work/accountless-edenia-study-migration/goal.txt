/goal Outcome: Deliver the accountless migration from https://bricechivu.github.io/Edenia/ to canonical https://www.edenia.study/ exactly as decided, with no silent loss or overwrite of browser-local progress. Existing users can recover normal-mode state through the disclosed legacy-origin helper and short-lived encrypted relay; new users reach current onboarding only after a conclusive no-state result or explicit defer. Authentication and cloud account sync remain optional and outside this work.

Context: Operate the work item at .agent/work/accountless-edenia-study-migration. Treat decision.md as the intent/provenance source and execplan.md as the executable contract governed by .agent/PLANS.md. Start from a refreshed remote base. Before coding, set meta.json to stage="implementation" and state="active". Use the work item as durable state across turns.

Boundaries: Execute execplan.md milestone by milestone in /Users/brice/Documents/Coding/Edenia. Build the reviewed standalone helper artifact and default-off app/relay changes, tests, docs, security controls, and rollout evidence described there. Preserve unrelated work. Read-only provider inspection is allowed when relevant. Do not push, open a PR, deploy Supabase, create/publish the helper repository, change Pages/DNS/provider settings, enable public migration, send external messages, or retire infrastructure without explicit user approval for that action.

Constraints: Keep old-origin bytes untouched; exclude sandbox/internal/auth/cache/analytics identity data; never put progress plaintext or raw capability in relay storage, logs, metrics, history, or analytics; never automatically write into a nonempty destination; verify and protect the incoming recovery backup before primary authority; keep manual import/recovery available; keep automatic migration, relay acceptance, and drain controls independently reversible. Preserve optional account behavior, current switch-off public behavior, five locales, exact-origin security, and the five-month plus rolling 90-day retirement rule. Reverify current Supabase docs/changelog and live configuration before provider-sensitive work.

Verify: After each meaningful slice run the nearest focused build/test and record it in execplan.md. Final evidence must cover relevant Node/Deno/Playwright suites, switch-off and failure cases, database grants/atomicity/advisors in an approved Supabase surface, old/new byte and normalized-hash checks, helper/relay privacy, cleanup/kill-drain behavior, browser/device results, and a fresh review-style cleanup pass. Production or real-device claims require matching live evidence; label proxy or unavailable checks honestly.

Iterate/done/stop: Keep Progress, Surprises & Discoveries, Decision Log, and Outcomes & Retrospective current. Choose the next incomplete safe milestone, implement the smallest reviewable slice, validate it, inspect the evidence, and revise execplan.md before continuing whenever evidence changes the path. Use focused local commits when useful, but no remote publication without approval. Do not mark complete because of elapsed time, budget exhaustion, partial implementation, compilation, or unrelated green checks. Done only after implementation, validation, and a fresh review satisfy both decision.md and execplan.md. If blocked by an approval, credential, real external state, or repeated technical barrier, preserve working behavior, record the exact blocker and evidence in execplan.md and meta.json, state what would unblock it, and stop honestly.
