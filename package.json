{
  "name": "edenia",
  "version": "1.0.0",
  "private": true,
  "description": "Browser-first language-learning progress app",
  "type": "module",
  "engines": {
    "node": ">=24.18.0 <25"
  },
  "scripts": {
    "build": "node scripts/build-site.mjs",
    "build:production": "npm run build && node scripts/write-runtime-config.mjs --require-key"
  },
  "devDependencies": {
    "esbuild": "0.28.1",
    "terser": "5.49.0"
  }
}
